worker_processes  1;

error_log  logs/error.log info;

events {
    worker_connections  1024;
}

stream {

	server {
			listen 80;
			proxy_pass stream_backend;
			}
	
	upstream stream_backend {
	server 172.27.192.1:8080;
	server 172.27.192.1:8081;	
	}
}

